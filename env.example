# Database Configuration for WAMP Server
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=
DB_NAME=atiera

# JWT Secret (change this in production!)
JWT_SECRET=atiera-super-secret-key-change-me-in-production

# Server Configuration
PORT=5050
NODE_ENV=development
